<!doctype html>
<html>
    <head>
        <title>Web Coordinator</title>
    </head>
    <body>
    [% IF message %]
    <p>[% message %]</p>
    [% END %]
        <dl>
            <dt><a href="[% c.uri_for('/testrun/list') %]">Testruns</a></dt>
                <dd><a href="[% c.uri_for('/testrun', 'new') %]">New</a></dd>
            <dt><a href="[% c.uri_for('/testsuite/list') %]">Testsuites</a></dt>
                <dd><a href="[% c.uri_for('/testsuite', 'new') %]">New</a></dd>
            <dt><a href="[% c.uri_for('/testcase') %]">Testcases</a></dt>
                <dd><a href="[% c.uri_for('/testcase', 'new') %]">New</a></dd>
        </dl>
        [% IF c.user %]
        Logged in as [% c.user.username %] ([% c.user.person.name %])
        <a href="[% c.uri_for('/logout') %]">Log out</a> 
        [% ELSE %]
        Not logged in. <a href="[% c.uri_for('/login') %]">Log in</a>.
        [% END %]
        <hr>
        [% content %]
        <hr>
        [% IF debug %]
        <p>[% debug %]</p>
        [% END %]
    </body>
</html>
